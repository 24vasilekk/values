{
  "name": "values-brand",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build needed for static site'",
    "start": "serve -s . -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}